import { ConstructorOptions } from '@logdna/logger'

/**
 * An extension of the {@link ConstructorOptions} interface provided by the `@logdna/logger`
 * library.
 */
export interface IMezmoOptions extends ConstructorOptions {}
