import { LogTransport } from '@interfaces/LogTransport.js'

/** The default number of hyphens to use when printing a divider. */
export const DEFAULT_DIVIDER_WIDTH = 25

/** The default transport used by the standard exported Logger. */
export const DEFAULT_LOGGER_TRANSPORTS: LogTransport[] = ['console']
